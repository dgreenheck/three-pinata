{
  "name": "three-pinata-monorepo",
  "private": true,
  "workspaces": [
    "app",
    "lib"
  ],
  "scripts": {
    "build:lib": "npm run build -w @dgreenheck/three-pinata",
    "build:demo": "npm run build -w three-pinata-demo",
    "dev": "npm run dev -w three-pinata-demo"
  }
}
